# Probly Web — Next.js frontend
# Stage 1: Build
FROM node:20-alpine AS builder

RUN corepack enable && corepack prepare pnpm@9.0.0 --activate

WORKDIR /app

COPY . .
RUN pnpm install --no-frozen-lockfile

# NEXT_PUBLIC_ vars must be set at build time
ARG NEXT_PUBLIC_API_URL
ARG NEXT_PUBLIC_BICONOMY_API_KEY
ARG NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID
ENV NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
ENV NEXT_PUBLIC_BICONOMY_API_KEY=${NEXT_PUBLIC_BICONOMY_API_KEY}
ENV NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=${NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID}

WORKDIR /app/apps/web
RUN npx next build

# Stage 2: Run
FROM node:20-alpine

WORKDIR /app

ENV NODE_ENV=production
ENV PORT=3000
ENV HOSTNAME="0.0.0.0"
EXPOSE 3000

# Copy standalone output (includes server.js + node_modules)
COPY --from=builder /app/apps/web/.next/standalone ./

# Copy static assets (NOT included in standalone)
COPY --from=builder /app/apps/web/.next/static ./apps/web/.next/static

# Copy public dir if it exists (create empty dir as fallback)
RUN mkdir -p ./apps/web/public
COPY --from=builder /app/apps/web/publi[c] ./apps/web/public

CMD ["node", "apps/web/server.js"]
