{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["cd ../.. && pnpm install --frozen-lockfile"]
    },
    "build": {
      "commands": ["pnpm --filter web build"]
    }
  },
  "deploy": {
    "startCommand": "pnpm --filter web start"
  }
}
